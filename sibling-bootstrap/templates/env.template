# drift-memory .env for agent @AGENT_NAME@
# Generated by sibling-bootstrap. Edit values here, not in code.

# MemoryDB (shared Postgres on host-only adapter)
DB_HOST=@HOST_IP@
DB_PORT=@DB_PORT@
DB_NAME=@DB_NAME@
DB_USER=@DB_USER@
DB_PASSWORD=@DB_PASSWORD@

# Routing — which schema this agent owns
DRIFT_DB_SCHEMA=@AGENT_NAME@
AGENT_SCHEMA=@AGENT_NAME@
AGENT_NAME=@AGENT_NAME@

# Service endpoints
OLLAMA_HOST=@HOST_IP@:@OLLAMA_PORT@
LOCAL_EMBEDDING_ENDPOINT=http://@HOST_IP@:@EMBED_PORT@/embed
NLI_SERVICE_URL=http://@HOST_IP@:@NLI_PORT@
CONSOLIDATION_DAEMON_URL=http://localhost:@CONSD_PORT@
